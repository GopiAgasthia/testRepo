kind: block
apiVersion: api.webmethods.io/beta
metadata:
  name: "ipBlock-sample"
  version: 1.0
  namespace: sample
spec:
  ip: #other type of criteria to block requests (headers?, other variants to be added)
    denyList: #also allowList, fromRedis -> fromRedis will either define a deny-list or an allow-list
      - "192.168.0.1"
      - "10.0.0.5"
      - "172.16.254.3"
      - "8.8.8.8"
      - "203.0.113.42"