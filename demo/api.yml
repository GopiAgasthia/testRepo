kind: api
apiVersion: api.webmethods.io/v1
metadata:
  name: Scoped_API
  namespace: dev
  version: 1.0
  tags:
    - sample
  type: REST
spec:
  api-spec:
    $path: petstore.yaml
  policy-sequence: 
    - $ref: dev:routing_policy:1.0
